version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly
  tests: true

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gosec
    - misspell
    - nilerr
    - nolintlint
    - revive
    - unconvert

formatters:
  enable:
    - gofmt
    - goimports
